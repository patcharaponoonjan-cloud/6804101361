# Playwright Testing แบบทีละตัวอย่าง

ตัวอย่างนี้ใช้เว็บไซต์

https://www.saucedemo.com/

เป้าหมายคือเรียนรู้ Playwright ทีละขั้น โดยแต่ละตัวอย่างประกอบด้วย

วัตถุประสงค์

↓

Code

↓

Run

↓

Expected Result

---

# ตัวอย่างที่ 1: ติดตั้ง Playwright

## วัตถุประสงค์

เตรียม Project สำหรับ Automated Testing ด้วย Playwright

## 1\. ตรวจสอบ Node.js

node \-v

npm \-v

ถ้าแสดง Version เช่น

v22.x.x

10.x.x

แสดงว่าพร้อมใช้งาน

## 2\. สร้าง Project

mkdir demo-playwright

cd demo-playwright

## 3\. ติดตั้ง Playwright

npm init playwright@latest

เลือก

TypeScript

tests

Install Playwright browsers \= Yes

## 4\. ตรวจสอบ Playwright

npx playwright \--version

ถ้าเห็น

Version 1.xx.x

แสดงว่าติดตั้งสำเร็จ

## 5\. ทดสอบ Project ครั้งแรก

npx playwright test

ถ้า Test ตัวอย่างที่ Playwright สร้างมาให้สามารถ Run ได้ แสดงว่า Environment พร้อมแล้ว

---

# ตัวอย่างที่ 2: เปิดเว็บไซต์

สร้างไฟล์

tests/01-open-page.spec.ts

## วัตถุประสงค์

ทดสอบว่า Playwright สามารถเปิดเว็บไซต์ SauceDemo ได้

## Code

import { test, expect } from '@playwright/test';

test('เปิดเว็บไซต์ SauceDemo', async ({ page }) \=\> {

  await page.goto('https://www.saucedemo.com/');

  await expect(page.locator('\#user-name')).toBeVisible();

});

## Run

npx playwright test tests/01-open-page.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 3: Login สำเร็จ

สร้างไฟล์

tests/02-login.spec.ts

## วัตถุประสงค์

ตรวจสอบว่า User ที่ถูกต้องสามารถ Login ได้

Username: standard\_user

Password: secret\_sauce

## Code

import { test, expect } from '@playwright/test';

test('Login สำเร็จ', async ({ page }) \=\> {

  // Arrange

  await page.goto('https://www.saucedemo.com/');

  // Act

  await page.locator('\#user-name').fill('standard\_user');

  await page.locator('\#password').fill('secret\_sauce');

  await page.locator('\#login-button').click();

  // Assert

  await expect(page).toHaveURL(/inventory\\.html/);

  await expect(page.locator('.inventory\_list')).toBeVisible();

});

## Run

npx playwright test tests/02-login.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 4: Login ไม่สำเร็จ

สร้างไฟล์

tests/03-login-error.spec.ts

## วัตถุประสงค์

ทดสอบ User ที่ถูก Lock

Username: locked\_out\_user

Password: secret\_sauce

## Code

import { test, expect } from '@playwright/test';

test('Locked user ไม่สามารถ Login ได้', async ({ page }) \=\> {

  // Arrange

  await page.goto('https://www.saucedemo.com/');

  // Act

  await page.locator('\#user-name').fill('locked\_out\_user');

  await page.locator('\#password').fill('secret\_sauce');

  await page.locator('\#login-button').click();

  // Assert

  await expect(page.locator('.error-message-container'))

    .toContainText('Sorry, this user has been locked out');

});

## Run

npx playwright test tests/03-login-error.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 5: เพิ่มสินค้าเข้า Cart

สร้างไฟล์

tests/04-add-cart.spec.ts

## วัตถุประสงค์

ทดสอบการเพิ่มสินค้า 1 รายการลง Shopping Cart

## Code

import { test, expect } from '@playwright/test';

test('เพิ่มสินค้าเข้า Cart', async ({ page }) \=\> {

  // Arrange

  await page.goto('https://www.saucedemo.com/');

  await page.locator('\#user-name').fill('standard\_user');

  await page.locator('\#password').fill('secret\_sauce');

  await page.locator('\#login-button').click();

  await expect(page.locator('.inventory\_list')).toBeVisible();

  // Act

  await page.locator('\[data-test="add-to-cart-sauce-labs-backpack"\]').click();

  // Assert

  await expect(page.locator('.shopping\_cart\_badge')).toHaveText('1');

});

## Run

npx playwright test tests/04-add-cart.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 6: ตรวจสอบสินค้าภายใน Cart

สร้างไฟล์

tests/05-check-cart.spec.ts

## วัตถุประสงค์

ตรวจว่าสินค้าที่เพิ่มเข้าไปอยู่ใน Cart จริง

## Code

import { test, expect } from '@playwright/test';

test('ตรวจสอบสินค้าใน Cart', async ({ page }) \=\> {

  // Arrange

  await page.goto('https://www.saucedemo.com/');

  await page.locator('\#user-name').fill('standard\_user');

  await page.locator('\#password').fill('secret\_sauce');

  await page.locator('\#login-button').click();

  // Act

  await page.locator('\[data-test="add-to-cart-sauce-labs-backpack"\]').click();

  await page.locator('.shopping\_cart\_link').click();

  // Assert

  await expect(page.locator('.inventory\_item\_name'))

    .toHaveText('Sauce Labs Backpack');

});

## Run

npx playwright test tests/05-check-cart.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 7: Sort ราคาจากต่ำไปสูง

สร้างไฟล์

tests/06-sort-price.spec.ts

## วัตถุประสงค์

ทดสอบว่าเมื่อเลือก Price (low to high) สินค้าเรียงราคาจากต่ำไปสูงจริง

## Code

import { test, expect } from '@playwright/test';

test('เรียงราคาจากต่ำไปสูง', async ({ page }) \=\> {

  // Arrange

  await page.goto('https://www.saucedemo.com/');

  await page.locator('\#user-name').fill('standard\_user');

  await page.locator('\#password').fill('secret\_sauce');

  await page.locator('\#login-button').click();

  await expect(page.locator('.inventory\_list')).toBeVisible();

  // Act

  const sort \= page.locator('\[data-test="product-sort-container"\]');

  await sort.selectOption('lohi');

  await expect(sort).toHaveValue('lohi');

  const priceText \= await page.locator('.inventory\_item\_price').allTextContents();

  const prices \= priceText.map(text \=\> Number(text.replace('$', '')));

  // Assert

  const sortedPrices \= \[...prices\].sort((a, b) \=\> a \- b);

  expect(prices).toEqual(sortedPrices);

});

## Run

npx playwright test tests/06-sort-price.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 8: Checkout

สร้างไฟล์

tests/07-checkout.spec.ts

## วัตถุประสงค์

ทดสอบกระบวนการซื้อสินค้าตั้งแต่ Login จนถึง Finish

## Code

import { test, expect } from '@playwright/test';

test('Checkout สินค้าสำเร็จ', async ({ page }) \=\> {

  // Arrange

  await page.goto('https://www.saucedemo.com/');

  await page.locator('\#user-name').fill('standard\_user');

  await page.locator('\#password').fill('secret\_sauce');

  await page.locator('\#login-button').click();

  await expect(page.locator('.inventory\_list')).toBeVisible();

  // Act

  await page.locator('\[data-test="add-to-cart-sauce-labs-backpack"\]').click();

  await page.locator('.shopping\_cart\_link').click();

  await expect(page.locator('.inventory\_item\_name')).toHaveText('Sauce Labs Backpack');

  await page.locator('\[data-test="checkout"\]').click();

  await page.locator('\#first-name').fill('John');

  await page.locator('\#last-name').fill('Doe');

  await page.locator('\#postal-code').fill('12345');

  await page.locator('\[data-test="continue"\]').click();

  await expect(page.locator('.summary\_info')).toBeVisible();

  await page.locator('\[data-test="finish"\]').click();

  // Assert

  await expect(page.locator('.complete-header'))

    .toHaveText('Thank you for your order\!');

});

## Run

npx playwright test tests/07-checkout.spec.ts

## Expected Result

1 passed

---

# ตัวอย่างที่ 9: Run Test ทั้งหมด

npx playwright test

## Expected Result

7 passed

---

# ตัวอย่างที่ 10: ดู Browser ขณะ Test

npx playwright test \--headed

---

# ตัวอย่างที่ 11: Debug Test

npx playwright test tests/06-sort-price.spec.ts \--debug

---

# ตัวอย่างที่ 12: ดู Test Report

npx playwright show-report

สามารถดูผล Passed, Failed, Duration, Error, Screenshot, Trace และ Video

---

# ตัวอย่างที่ 13: Re-test หลังแก้ Error

Run เฉพาะ Test ที่ Failed ก่อน

npx playwright test tests/06-sort-price.spec.ts

ถ้าผ่านแล้วจึง Run ทั้งหมดอีกครั้ง

npx playwright test

---

# สรุปลำดับการเรียนรู้

1\. ติดตั้ง Playwright

        ↓

2\. เปิด Website

        ↓

3\. Login สำเร็จ

        ↓

4\. Login Error

        ↓

5\. Add Cart

        ↓

6\. ตรวจสินค้าใน Cart

        ↓

7\. Sort Product

        ↓

8\. Checkout

        ↓

9\. Run Test ทั้งหมด

        ↓

10\. Headed Mode

        ↓

11\. Debug

        ↓

12\. Test Report

        ↓

13\. Re-test

# หลักสำคัญของแต่ละ Test

แต่ละ Test ควรประกอบด้วย

Arrange

↓

Act

↓

Assert

ตัวอย่าง

test('ตัวอย่าง Test', async ({ page }) \=\> {

  // Arrange

  // เตรียมข้อมูลและสถานะ

  // Act

  // ทำสิ่งที่ต้องการทดสอบ

  // Assert

  // ตรวจสอบผลลัพธ์

});

แนวทางนี้ทำให้ Test แต่ละตัวอ่านง่าย แยกจากกัน และสามารถทดลองได้อย่างสมบูรณ์ทีละกรณี  
